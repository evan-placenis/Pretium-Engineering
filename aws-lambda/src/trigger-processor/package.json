{
  "name": "pretium-trigger-processor",
  "version": "1.0.0",
  "description": "Trigger processor Lambda function for Pretium",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node18 --outfile=dist/index.js --external:@aws-sdk/*",
    "dev": "ts-node index.ts",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.614.0",
    "@supabase/supabase-js": "^2.49.4"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "esbuild": "^0.19.0",
    "ts-node": "^10.9.0"
  }
} 